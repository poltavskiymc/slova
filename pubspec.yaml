name: slova
description: The game words on the forehead
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  sqflite: ^2.3.0
  path: ^1.8.3
  riverpod: ^2.5.1
  flutter_riverpod: ^2.5.1
  sensors_plus: ^4.0.1
  audioplayers: ^5.2.1
  shared_preferences: ^2.2.2
  supabase_flutter: ^2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/audio/

